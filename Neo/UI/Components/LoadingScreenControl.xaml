<UserControl x:Class="Neo.UI.Components.LoadingScreenControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"></RowDefinition>
            <RowDefinition Height="1*"></RowDefinition>
        </Grid.RowDefinitions>
        <Border VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Grid.RowSpan="2">
            <Image x:Name="LoadingScreenImage"></Image>
        </Border>
        <Border VerticalAlignment="Center" Grid.Row="1" x:Name="LoadingFillBorder">
            <Image VerticalAlignment="Center" HorizontalAlignment="Left" x:Name="LoadingScreenBarFillImage"></Image>
        </Border>
        <Image VerticalAlignment="Center" Grid.Row="1" x:Name="LoadingScreenBarImage"></Image>
    </Grid>
</UserControl>
